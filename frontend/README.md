# Frontend My Cloud

React SPA проекта My Cloud. Для разработки: `npm ci && npm run dev`. Для production: `npm ci && npm run build`; Vite собирает приложение в `../backend/frontend_dist`, после чего Django обслуживает SPA и API одним сервером. Полная инструкция — в корневом `README.md`.
